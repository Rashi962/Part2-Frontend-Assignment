// Original array declaration
const s = [5, 7, 2];

// Mutate the array elements
s[0] = 2;  // Change the first element
s[1] = 5;  // Change the second element
s[2] = 7;  // Change the third element

// Output the mutated array
console.log(s);  // [2, 5, 7]
